# --- General Server Config ---
server.port=8080
spring.application.name=sentinel-backend

# --- Database (PostgreSQL) ---
spring.datasource.url=jdbc:postgresql://localhost:5432/sentinel_db
spring.datasource.username=user
spring.datasource.password=password
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# --- Kafka Producer Config ---
# Points to the Kafka broker running in Docker
spring.kafka.bootstrap-servers=localhost:9092

# Serializers tell Kafka how to turn Java Objects into Bytes
spring.kafka.producer.key-serializer=org.apache.kafka.common.serialization.StringSerializer
spring.kafka.producer.value-serializer=org.springframework.kafka.support.serializer.JsonSerializer

# --- Kafka Consumer Config (For receiving decisions back from Python) ---
spring.kafka.consumer.group-id=sentinel-group
spring.kafka.consumer.auto-offset-reset=earliest
spring.kafka.consumer.key-deserializer=org.apache.kafka.common.serialization.StringDeserializer
spring.kafka.consumer.value-deserializer=org.springframework.kafka.support.serializer.JsonDeserializer

# Essential: Allows Spring to trust the JSON packets coming from the Python service
spring.kafka.consumer.properties.spring.json.trusted.packages=*

# --- Observability & Monitoring ---
# Exposes the /actuator/prometheus endpoint for our Monitoring stack
management.endpoints.web.exposure.include=health,info,prometheus
management.prometheus.metrics.export.enabled=true
management.endpoint.health.show-details=always